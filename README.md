# pyscrcpy
A python [scrcpy](https://github.com/Genymobile/scrcpy) client implementation for simple api usage

# Usage

See [`threaded_example.py`]() for a threaded example and [`unthreaded_example.py`]() for a unthreaded example.
